- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.profile: profile
    ~/.bashrc: bashrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
