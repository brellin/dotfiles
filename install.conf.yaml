- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.profile: profile
    ~/.bashrc: bashrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
